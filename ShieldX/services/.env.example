# Application Configuration
APP_NAME=ShieldX Services
APP_VERSION=0.1.0
DEBUG=false
ENVIRONMENT=development

# API Configuration
API_PREFIX=/api
API_VERSION=v1

# Server Configuration
HOST=0.0.0.0
PORT=8000
RELOAD=false

# Logging
LOG_LEVEL=INFO

# CORS Configuration
CORS_ORIGINS=["*"]
CORS_CREDENTIALS=true
CORS_METHODS=["*"]
CORS_HEADERS=["*"]
